name: Update Profile Stats
on:
  schedule:
    - cron: '0 0 * * *' # Runs daily at midnight UTC
  workflow_dispatch:

jobs:
  update-readme:
    name: Update README
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Update GitHub Stats
        # A simple action to trigger any automated stat regenerations or just keep activity alive
        run: echo "Repository stats successfully synchronized."

      # Keep alive
      - uses: gautamkrishnar/keepalive-workflow@v1
